/** Client-side MIDI clock synchronized to transport — Phase 5+. */
export class MidiClock {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  constructor(_options?: Record<string, unknown>) {}
}
